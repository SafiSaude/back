# Server Configuration
NODE_ENV=development
PORT=3001
API_PREFIX=api
LOG_LEVEL=debug

# Supabase Configuration (2026 Format)
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# JWT Configuration
# Use custom JWT secret or leave as Supabase-managed
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRATION=3600

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Database (if using local PostgreSQL instead of Supabase)
# Leave empty if using Supabase PostgreSQL
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_NAME=safisaude

# Python Scripts
PYTHON_SCRIPTS_PATH=../../scripts

# DEPRECATED - Do not use these in new code:
# SUPABASE_ANON_KEY=xxx (old format, replaced by PUBLISHABLE_KEY)
# SUPABASE_SERVICE_ROLE_KEY=xxx (old format, replaced by SECRET_KEY)
